[project]
name = "simulators"
version = "0.1.0"
description = "Event simulators for the data platform"
requires-python = ">=3.12"
dependencies = [
    "faker>=26.0.0",
    "pyyaml>=6.0",
    "requests>=2.32.0",
]

