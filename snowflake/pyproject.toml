[project]
name = "dataplatform-snowflake"
version = "0.1.0"
description = "schemachange-managed Snowflake DDL for the AWS + Snowflake data platform demo"
requires-python = ">=3.11"
dependencies = [
    "schemachange>=3.7.0,<4.0.0",
]

[tool.uv]
package = false
